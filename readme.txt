Welcome to the snippetJS repo!

to get started, you will need to run 'npm install' to grab the needed dependencies, then run the 'npm run dev-build' to generate the build folder

feel free to run the server start script in the package.json to test/run the application, otherwise you should be set!